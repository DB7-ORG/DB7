SPECIFICATION Spec
CONSTANTS
    NUM_THREADS = 1
    VALUES = 2


\* \* allocate new node
    \* new_node := nodes[next_free];
    \* next_free := next_free+1;   
    \* \* copy data
    \* old_mid := cur.count \div 2 + 1;
    \* mid:=old_mid;
    \* new_node_idx := 1;
    \* while mid<=cur.count do 
    \*     new_node.keys[new_node_idx] := cur.keys[mid];
    \*     mid:=mid+1;
    \*     new_node_idx:=new_node_idx+1;
    \* end while;

    \* \* set links to new node
    \* new_node.rlink := cur.rlink;
    \* cur.rlink := new_node.idx;
    \* old_count := cur.count;
    \* cur.count := cur.count \div 2;
    \* new_node.count := old_count - (old_mid - 1);

    \* if idx <= old_mid then
    \*     shift_and_insert(cur, cur.keys, idx, key);
    \* else
    \*     shift_and_insert(new_node, new_node.keys, idx  - cur.count, key);
    \* end if;

    \* seperator := cur.keys[cur.count];
    \* prev_node := cur.idx;

    \* mid := old_mid;
    \* new_node_idx := 1;
    \* while mid <= cur.count do
    \*     new_node.keys[new_node_idx] := cur.keys[mid];
    \*     new_node.children[new_node_idx] := cur.children[mid];
    \*     mid := mid + 1;
    \*     new_node_idx := new_node_idx + 1;
    \* end while;
    \* new_node.children[new_node_idx] := cur.children[mid];